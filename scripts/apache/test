#!/usr/bin/perl -Tw

# Author:        jgrg
# Group:         anacode
# Maintainer:    jgrg

use strict;
use warnings;

BEGIN { use lib ($ENV{OTTER_PERL_INC} || q{}) =~ m{([^:]+)}g }
use SangerPaths qw{ core bioperl123 ensembl73 otter78 humpub };

use Bio::Otter::ServerAction::Test;
use Bio::Otter::Server::Support::Web;

sub test {
    my ($server) = @_;

    my $test = Bio::Otter::ServerAction::Test->new($server);

    return $test->as_yaml;
}

Bio::Otter::Server::Support::Web->send_response(\&test);
